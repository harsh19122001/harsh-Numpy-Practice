# NumPy Practice – Complete Notes & Code Examples

This repository contains my complete practice work for **NumPy**, one of the core Python libraries for numerical computing and data manipulation.  
I’ve covered all fundamental to advanced concepts with examples, explanations, and exercises.

---

## 📌 Topics Covered

### ✔ 1. NumPy Basics
- Introduction to NumPy
- Why NumPy is faster than Python lists
- Creating arrays (1D, 2D, 3D)
- `ndarray` object structure

### ✔ 2. Array Creation Methods
- `np.array()`
- `np.arange()`
- `np.linspace()`
- `np.zeros()`, `np.ones()`, `np.eye()`
- Random arrays: `rand`, `randn`, `randint`

### ✔ 3. Array Attributes
- `shape`
- `ndim`
- `dtype`
- `size`
- `itemsize`

### ✔ 4. Indexing & Slicing
- Basic indexing
- Slicing ranges
- Step slicing
- Negative indexing
- 2D row/column extraction

### ✔ 5. Mathematical Operations
- Element-wise operations
- Arithmetic operations
- Universal functions (ufuncs)
- Aggregate functions: `sum`, `mean`, `std`, `min`, `max`, `argmin`, `argmax`

### ✔ 6. Boolean Indexing
- Filtering arrays
- Applying conditions
- Combining conditions using &, |

### ✔ 7. Reshaping Arrays
- `reshape()`
- `ravel()`
- `flatten()`
- `transpose()`
- `swapaxes()`

### ✔ 8. Array Manipulation
- `concatenate`
- `vstack`, `hstack`
- `split`, `hsplit`, `vsplit`

### ✔ 9. Broadcasting
- How broadcasting works internally
- Rules of broadcasting
- Scalar vs vector vs matrix operations

### ✔ 10. Random Module
- Random number generation
- `np.random.seed()`
- Shuffling + permutation



---

## 📂 Structure of This Repository
